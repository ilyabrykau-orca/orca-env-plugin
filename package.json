{
  "name": "claude-toolkit",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bash build.sh",
    "typecheck": "bun run --bun tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8"
  }
}
